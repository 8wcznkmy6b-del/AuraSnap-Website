# AuraSnap 完整功能与使用指南全集 (AuraSnap Feature & Usage Manual)

> 本文档根据 AuraSnap 源码（`Sources/`）与最新版本（v2.5.4 ~ v2.9.0）全景梳理，涵盖所有截图、贴图、录屏、长截图、高可用多语言 OCR 文字识别、文本清洗套件、单键标注快捷键架构、马赛克区域均值像素化、智能内容擦除 (Content Eraser)、独立背景美化工作台 (Background Beautifier)、贴纸与标注手势系统、AI 视觉与流式解析、抠图净底、水印滤镜、引线批注、分模块独立主题配置、菜单栏与工具栏定制、快捷键体系及底层交互手势。

---

## 目录
1. [屏幕截图与窗口/微观元素探测](#1-屏幕截图与窗口微观元素探测)
2. [独立背景美化工作台与 5K 系统壁纸 (Background Beautifier)](#2-独立背景美化工作台与-5k-系统壁纸-background-beautifier)
3. [智能内容擦除与无痕消除 (Content Eraser)](#3-智能内容擦除与无痕消除-content-eraser)
4. [屏幕贴图与深度交互 (Pin Window)](#4-屏幕贴图与深度交互-pin-window)
5. [剪贴板图文/代码极速转贴图 (Text/Image-to-Pin)](#5-剪贴板图文代码极速转贴图-textimage-to-pin)
6. [区域录屏与多格式视频/动图导出](#6-区域录屏与多格式视频动图导出)
7. [双引擎长截图与 Retina 2x Minimap](#7-双引擎长截图与-retina-2x-minimap)
8. [独立 OCR 文字识别、空间几何排版与全能文本清洗套件 (OCR Studio)](#8-独立-ocr-文字识别空间几何排版与全能文本清洗套件-ocr-studio)
9. [矢量标注、贴纸系统与智能图形体系](#9-矢量标注贴纸系统与智能图形体系)
10. [全局平铺防泄密水印滤镜 (Global Watermark)](#10-全局平铺防泄密水印滤镜-global-watermark)
11. [一键智能抠图与净底工作台 (Bria AI / 拉普拉斯 / Apple Vision)](#11-一键智能抠图与净底工作台)
12. [AuraVision AI 视觉大模型与输入清洗 (DeepSeek / Claude / Ollama)](#12-auravision-ai-视觉大模型与输入清洗)
13. [分模块独立主题配置与高对比 HUD 渲染 (Liquid Glass / HUD / Classic)](#13-分模块独立主题配置与高对比-hud-渲染)
14. [工具栏与菜单栏深度定制 (数据驱动排序与智能直达模式)](#14-工具栏与菜单栏深度定制)
15. [全维度快捷键体系与单键直达系统](#15-全维度快捷键体系与单键直达系统)
16. [历史图库画廊与离线 OCR 语义检索](#16-历史图库画廊与离线-ocr-语义检索)
17. [快捷键与高频交互手势全览表](#17-快捷键与高频交互手势全览表)
18. [原地截图翻译 (In-Place Screenshot Translation)](#18-原地截图翻译-in-place-screenshot-translation)
19. [AuraSnap Pro 会员与免费配额 (Pro Membership & Free Quota)](#19-aurasnap-pro-会员与免费配额-pro-membership--free-quota)
20. [径向分屏管理与智能邻接贴合 (Radial Window Manager)](#20-径向分屏管理与智能邻接贴合-radial-window-manager)
21. [鼠标增强、平滑滚动与线性指针 (Mouse & Cursor Enhancement)](#21-鼠标增强平滑滚动与线性指针-mouse--cursor-enhancement)

---

## 1. 屏幕截图与窗口/微观元素探测

### 1.1 触发与自适应变暗
- **全局快捷键**：默认 `⌥ + 1`（`Option + 1`），支持在设置中自定义录制任意组合键。
- **全屏原画无损变暗**：按下快捷键瞬间冻结屏幕并压暗背景；只要鼠标发生位移（> 3pt），全屏瞬间恢复 100% 原始色彩与亮度，拖拽画出选区后选区外部重新变暗，确保框选与取色绝对零色差。
- **统一转场协调器 (v2.7.6)**：全局接入 `WindowTransitionCoordinator`，统一定义 macOS 黄金转场度量 `120ms`，入场与退场平滑淡入淡出，消除生硬硬切与视觉闪烁。
- **并发防重入锁与图层防抖 (v2.7.6)**：底层引入主线程并发防重入锁，彻底拦截高频狂按快捷键导致的重复建窗；暗色遮罩透明度稳定防抖，消除鼠标微动引发的明暗跳变。

### 1.2 宏观窗口与微观 UI 元素智能探测
- **自动吸附**：鼠标悬停在任意 App 上自动高亮窗口边界。
- **空格键 (`Space`) 切换宏微观逻辑**：
  - 默认模式：吸附整个应用主窗口（宏观）。
  - 按下 `Space`：切换为精确吸附按钮、输入框、Tab 栏、列表项等局部 UI 控件（基于 `AXUIElement` 深度探测）。
- **滚轮结构树导航**：悬停时滑动鼠标滚轮，可在 UI 层级树中向上或向下穿梭（父容器 $\leftrightarrow$ 子控件切换）。
- **下拉菜单防卡死逃逸 (v2.6.6)**：当打开系统下拉菜单（`NSMenu` / `Picker`）处于事件追踪模式时，触发快捷键会自动安全逃逸菜单追踪（RunLoop Escape），彻底消灭截图卡死。
- **开关设置**：在偏好设置中可随时开启或关闭窗口探测。

### 1.3 极客取色器与纯圆透镜放大镜
- **反色全屏十字线**：全屏十字交叉标尺自带底片反色差分效果（`.difference`），在纯黑纯白背景下均清晰可见。
- **纯圆弥散悬浮透镜**：21x21 物理像素点对点超清放大，带中心十字标尺。
- **极限边界智能避让与 Clamp 保护**：透镜永远躲在十字线夹角内，鼠标贴死在屏幕物理边缘（0, 0）时自动收敛，绝不超出屏幕，取色胶囊永不消失。
- **取色与快捷复制**：
  - 按 `C` 键：直接复制当前光标处的 `HEX` 与 `RGB` 色值到剪贴板，并弹出 Toast 提示。
  - 方向键 `↑ ↓ ← →`：以 1px / 1pt 为步进精准微调取色准星位置。

---

## 2. 独立背景美化工作台与 5K 系统壁纸 (Background Beautifier)

### 2.1 极速独立唤醒与 680pt 黄金控制岛 (v2.9.0)
- **快捷唤醒**：框选截图后点击工具栏【背景美化】按钮（快捷键 **`F`** 或全局 **`⌥ + 7`**），瞬间弹出独立自适应美化窗口。
- **680pt 黄金控制岛**：美化模式（壁纸/调色板/光影）与标注模式（10 款标注工具原位平铺、二级属性弹性菜单与线宽动态预览）无缝互切，在各类显示器分辨率下保证 0 挤压、0 裁切。
- **原生 macOS 显式横向滚动条**：底部预设库集成原生横向滚动指示器，完美支持触控板 120Hz 双指惯性滑动、鼠标滚轮与滑块拖拽。

### 2.2 真实 5K 系统壁纸与智能萃取色板
- **真实系统桌面壁纸一键铺底**：通过 `WallpaperProvider` 一键捕获当前物理屏幕壁纸，并自动扫描系统 `/System/Library/Desktop Pictures/` 下官方 5K/6K HEIC 极清原图（Sequoia / Sonoma / Ventura 等）；通过 `CGImageSourceCreateThumbnailAtIndex` 异步下采样与 120MB LRU 显存缓存，0 模糊且切换丝滑。
- **智能主色提取 (K-Means Clustering)**：基于 `DominantColorExtractor` 自动提取当前截图 4 款主色调，提供「经典中性灰阶」、「莫兰迪低饱和色」、「Apple 经典硬件色」与系统原生 `ColorPicker`。
- **3D 浮空光影与 CGImageDestination 流式导出**：修正物理高斯衰减阴影留白（32 / 64 / 96pt），流式导出超清 PNG，彻底消除显存膨胀与深色边缘切边。

### 2.3 无残影秒开返回与双向标注同步
- **秒开返回截图**：进入美化窗口时冻结截屏帧，点击「返回截图」直接复用内存帧秒级返回，彻底消除双层工具栏冲突与窗口残影。
- **双向无损标注同步**：在美化窗口中绘制的最新箭头、文字、矩形与涂鸦，返回截图或直接导出时保持 100% 完整保留。

---

## 3. 智能内容擦除与无痕消除 (Content Eraser)

### 3.1 自适应双模 Inpainting 消除算法 (v2.9.0)
- **直方图背景主色分离 (Dominant Background Mode Clustering)**：自动对选区边缘采样像素进行色彩密度聚类，强制剔除所有深色文字笔画、标点符号（如 `•`）与微小噪点，确保浅色/白底背景消除后 100% 洁白透亮。
- **正交纯净基线插值与双线性渐变拟合**：提取选区上下左右纯净基线，自适应还原窗口自然纵向光影与渐变。
- **PatchMatch 结构化纹理合成 (Barnes SIGGRAPH 2009)**：遭遇复杂微纹理、桌面材质或壁纸时，自动搜索最佳 $5\times 5$ 纯净图块进行克隆拼合。
- **掩码 Alpha 隔离与 2.0px 亚像素羽化**：补丁图像除消除选区外 100% 完全透明，彻底根除“方形画布覆盖/贴膏药”的矩形边界硬切感。

### 3.2 三种选区形态与即时确认
- **快捷键直达**：点击工具栏【橡皮擦】按钮或按下 **`D`** 键。
- **多模态选区**：支持 ▭ 矩形框选（默认）、⭘ 自由套索圈选 与 ✎ 画笔涂抹三种形态，松手立即消除画面且不留手柄阻挡视线。
- **纯值快照秒级撤销**：支持 `⌘ + Z` 极速单步撤销，无损恢复原始图像。

---

## 4. 屏幕贴图与深度交互 (Pin Window)

### 4.1 基础与贴图生命周期
- **生成贴图**：选区完成后点击工具栏【贴图】按钮或快捷键（默认 `⌘ + P`），以 `.floating` 级别浮窗置顶在所有应用最上层。
- **双击关闭**：双击贴图任意空白处瞬间销毁窗口（手写事件响应，彻底绕过系统级双击拦截延迟）。
- **中键一键重置**：鼠标中键点击贴图，立即恢复 100% 原始缩放与不透明度。
- **多张贴图并发**：支持在多块屏幕上同时置顶几十张贴图，每张贴图独立记忆其缩放尺寸、圆角与阴影。

### 2.2 深度交互特技与拖拽外发
- **按住 `⌘ (Command)` 直接拖拽外发与保存**：
  - **拖进通讯软件发送**：在贴图上**按住 Command 键不放直接拖拽**，即可将贴图直接拖入**微信、钉钉、飞书、Slack、Telegram、邮件**等聊天输入框，瞬间以图片形式发送！
  - **拖入文件夹保存**：按住 ⌘ 拖拽贴图至桌面或访达（Finder）任意文件夹，即刻自动保存为高清 PNG 图片文件，无需点击保存按钮或选择路径弹窗。
  - 普通不按 Command 键拖拽则为在屏幕上移动贴图窗口位置。
- **无极缩放与未截断浮点时间线 (v2.7.3 重构)**：
  - 鼠标滚轮 / 触控板双指捏合：以鼠标指针所在位置为锚点进行 0.3x ~ 10.0x 无级等比缩放。
  - **彻底攻克 macOS 坐标整形截断难题**：引入 `unroundedFrame` 无限精度浮点时间线追踪，彻底消除 macOS 底层 WindowServer 整数像素截断（Integral Snapping）导致的滚雪球累加漂移，实现无论如何拉伸缩放都纹丝不动的 120Hz 极稳平滑缩放。
- **透明度无极调节**：
  - 按住 `⌥ (Option)` + 滚动滚轮：在 10% ~ 100% 之间调节贴图透明度。
- **Ghost Mode 穿透模式 (Click-Through)**：
  - 贴图右键菜单或快捷键（默认 `⌥ + G`）开启“穿透模式”后，贴图窗口完全忽略鼠标点击，鼠标可直接穿透贴图去点击和编辑下方的代码与网页。
  - **长按 `⌥ (Option)` 临时唤醒**：在穿透状态下，长按 Option 键可临时接管贴图控制权，支持移动和调整，松开即刻恢复穿透。
- **全域浮窗键盘快捷键系统 (v2.7.1 新增)**：
  - 贴图窗口直接支持 `⌘ + C`（复制）、`⌘ + S`（保存）、`⌘ + P`（转贴图）、`Esc`（关闭）等键盘快捷键。
  - **前置焦点隔离**：在文字批注编辑状态下自动放行打字输入，打字完成即刻恢复窗口热键。
- **跨空间全屏穿透特权 (v2.7.2 新增)**：
  - 注入系统级 `[.fullScreenAuxiliary, .canJoinAllSpaces, .stationary]` 权限，贴图可自由悬浮在全屏 Safari、Xcode 等全屏应用空间之上。
- **永久非破坏性二次编辑与双轨物理隔离**：
  - 贴图自带专属浮动工具栏（`PinToolbarView`）。底层强制采用 **Unflattened 纯净底图 + Live Annotations 注入**，随时可再次涂鸦、画箭头、打马赛克、修改已有文本或删除序号，绝无双重叠底底图污染，并支持 `⌘ + Z` 撤销。
  - **每帧绝对坐标物理锚定 (v2.7.0)**：工具栏基于父窗口物理边界实时推导数学绝对坐标，彻底消除尺寸过渡动画与手动拖拽产生的机械抖动与抽搐。
- **离线 OCR 文本提取与靶向分流**：
  - 贴图支持在工具栏点击 🔍 图标或按下 `⌥ + O` 提取文字并弹出独立 OCR 结果窗口。
  - 鼠标移到 Live Text 文字处划选后按 `⌘ + C`，靶向精准复制文字内容，不破坏整图复制逻辑。
- **贴图误关恢复栈 (`⌥ + 2`)**：
  - 贴图关闭时以 PNG 形式存入临时磁盘缓存（Disk-backed Pins，内存占用彻底归零）。
  - 按下 `⌥ + 2`（`Option + 2`），系统会按后进先出栈自动恢复最近关闭的贴图窗口（支持在设置中自定义最大恢复层数）。

---

## 3. 剪贴板图文/代码极速转贴图 (Text/Image-to-Pin)

### 3.1 一键极速文本/图片贴图 (`⌘ + ⌥ + T`)
- **零截图直出**：无需手动框选或打开备忘录。复制一段报错日志、API 密钥、JSON、代码片段或**外部图片**后，按下全局快捷键 **`⌘ + ⌥ + T`**（`Cmd + Option + T`，支持自定义），屏幕上瞬间生成一张排版精美的浮空贴图！
- **图文双模态智能分流 (v2.7.5 升级)**：
  - **复制文本/代码**：自动转换为高清毛玻璃纯文本贴图。
  - **复制外部图片**：自动走独立图片转贴图管线，无缝上屏。

### 3.2 极客原生视觉与细节优化
- **剪贴板外部图片 Alpha Trimming 与 Retina 降维 (v2.7.3 新增)**：
  - 针对外部复制的图片自动执行透明边缘裁切算法（Alpha Trimming），清除多余空白留白；
  - 引入 `backingScaleFactor` 降维缩放与 `visibleFrame` 限幅居中保护，杜绝超大图片撑爆屏幕。
- **纯文本贴图空白处穿透拖拽 (v2.7.3 修复)**：
  - 引入字形级物理轮廓检测（Glyph-Level Hit Shielding），在文本行间或空白区域点击可直接穿透触发窗口拖拽移动，文字区域则精准保留高亮划选。
- **原生毛玻璃质感 (Frosted Glass UI)**：采用原生 SwiftUI `NSTextView` 混排架构与 `NSVisualEffectView` 半透明材质，完整保留次像素抗锯齿 (Subpixel AA)，文字清晰锐利。
- **智能多屏光标居中**：自动探测当前鼠标所在的物理显示器，将文本贴图精准居中呈现。
- **10,000pt 超大文本安全截断与撕裂渐变**：对超长日志或几百万字复制内容内置 1 万像素物理安全防线，底部呈现“锯齿撕裂 + 透明渐变”视觉效果，保护 GPU 显存不溢出。
- **双轨解耦兼容马赛克批注**：前台为原生文本，后台自动压制纯白底高质量 `CGImage`，支持在文本贴图上直接进行高斯模糊、马赛克、箭头与画笔二次批注。

---

## 4. 区域录屏与多格式视频/动图导出

### 4.1 三种专业格式支持
- **MP4**：基于 Apple 原生硬件加速，色彩保真度极高，适合日常分享与长视频。
- **GIF**：内置极客级 `GIFEncoder`，动态调色板量化算法，彻底消灭色彩断层与掉帧卡顿。
- **WebM**：集成 FFmpeg 引擎支持 VP9 编码，以极小体积实现高清画质。

### 4.2 独创交互与硬件级同步
- **录制流程**：框选区域 $\to$ 呼出录屏控制栏 $\to$ 开始录制 $\to$ 暂停/继续 $\to$ 完成录制。
- **录完即看**：录制完成后立刻弹出 `GIFViewerPanel` 悬浮循环播放器。
- **按住 `⌘ (Command)` 直接拖拽**：
  - 在播放器窗口内，**按住 Command 键不放直接拖拽**，即可将生成的 MP4/GIF/WebM 文件直接拖入**微信、钉钉、飞书、Telegram、Slack、邮件**输入框，或直接拖到桌面/访达文件夹保存！无需经历繁琐的“另存为”弹窗。
- **全域浮窗键盘快捷键与富文本剪贴板注水 (v2.7.1 新增)**：
  - 播放器窗口支持 `⌘ + C`（复制动图）、`⌘ + S`（保存文件）、`Esc`（关闭）。
  - **二进制数据流显式注水**：复制 GIF 时自动向剪贴板注入 `com.compuserve.gif` 二进制数据流，粘贴至微信、飞书等软件时直接发送动图而非本地文件路径。
- **120Hz ProMotion 级硬件原子同步拖拽 (v2.7.2 新增)**：
  - 拖拽录屏预览窗口时，位移在 `CATransaction` 容器内打包提交，浮动工具栏与主播放器在同一垂直同步周期内实现 120Hz 绝对跟随，彻底消除卡顿脱节。
- **核心渲染树与转码物理隔离 (v2.7.2)**：播放引擎图层采用 `.equatable()` 与 `layerContentsRedrawPolicy = .never` 隔离，后台高速转码写盘时画面 0 黑帧、0 频闪。
- **冷启动孤立缓存前置扫荡**：App 启动时自动清理历史残留的录屏临时切片，杜绝磁盘垃圾堆积。

---

## 5. 双引擎长截图与 Retina 2x Minimap

### 5.1 核心攻克与算法架构
- **快滑防截断**：4 态有限状态机（FSM）+ 滑动窗口有向无环图（DAG, $W=4$）+ 光束搜索，遇极限断代自动插入 Tear Indicator 并开辟新瓦片。
- **慢滑防重复**：自相关周期性熵 + Apple Accelerate 硬件级 2D DP 接缝切取（`vDSP_vmin` 单帧计算 $<1.5\text{ms}$）+ OMB 重叠区融合块。
- **纯净图层穿透抓取与零闪烁 (v2.7.6 重构)**：
  - 基于 WindowID 穿透全屏遮罩直接捕获真实物理底图，彻底废除 Alpha 篡改代码，开录瞬间 0 闪烁；
  - 严格对齐 `stop()` 与 `finishAndStitch()` 生命周期，消除点击确定/取消时的遮罩黑闪与旧底图回闪。
- **终态物理帧同步捕获**：点击完成时先执行 `CATransaction.flush()` 强制提交图层并捕获最终物理帧，杜绝最后一行内容漏截。

### 5.2 实时微缩地图与交互滚动条
- **Retina 2x Minimap**：宽度固定 160pt（320px Retina 物理像素），高度随滑动自顶向下自然生长，底端通过量化整数边界严格锚定，零黑屏、零横纹。
- **真实双向交互滚动条**：
  - 4px 极细胶囊美学，36pt 物理防坍缩。
  - 按住滚动条可高速拖拽翻页；点击轨道空白处平滑跳页。
  - **按住 `Shift` 慢速精准微调模式**：自动施加 1/4 阻尼，结合动态重锚定（Anchor Re-baselining）算法，切换 Shift 瞬间画面绝对不跳跃。
- **全域浮窗键盘快捷键 (v2.7.1 新增)**：长截图预览面板直接支持 `⌘ + C`（复制长图）、`⌘ + S`（保存长图）、`⌘ + P`（固定为贴图）、`Esc`（退出）。

---

## 6. 独立 OCR 文字识别、空间几何排版与全能文本清洗套件 (OCR Studio) `[v2.7.4 ~ v2.8.0 核心升级]`

### 6.1 高可用多语言 OCR 引擎与毫秒级识别 (v2.8.0 架构升级)
- **规范化 BCP-47 语言映射与全语系提示池 (BCP-47 Normalizer & Hint Pool)**：
  - 针对系统语言代码（如 `zh-Hans-CN`、`zh-Hant-HK`）自动剥离地区后缀，规范映射为标准代码（`zh-Hans` / `zh-Hant`）。
  - 构建「用户偏好 + 中英日韩基础主力语种」全语系提示池，彻底杜绝向 Apple CDN 误请求未安装冷门语言包引发的 15~30 秒网络阻塞与乱码，实现 100% 离线本地零网络请求。
- **macOS 13+ Apple Neural Engine 硬件级多语言混排加速**：
  - 全面启用 `automaticallyDetectsLanguage = true`，自动调度 Apple Silicon 统一多语系神经网络模型，中英日韩德法西等混排识别速度进入百毫秒级（**0.1 ~ 0.4 秒瞬发直出**）。
- **超大 Retina 4K/5K/6K 分辨率等比尺寸防御 (Scale-Invariant Downsampling)**：
  - 对长边超过 4096px 的超大截图在内存中执行严格等比平滑缩放，归一化坐标与宽高比（`relativeGap = gapX * aspectRatio`）保持 100% 同构，空间几何排版零错位，保护 GPU 与 Neural Engine 内存不溢出。
- **两阶段容灾与保底降级通道 (Two-Tier Fallback Pipeline)**：
  - 若主语义通道遇极端异常抛错，系统毫秒级自动降级至 `.fast` 纯视觉字形提取通道（关闭语言纠错 `usesLanguageCorrection = false`），确保 **100% 成功产出文本**。
- **渐进式无感状态提示流 HUD (Progressive Feedback HUD)**：
  - 耗时 < 400ms 任务瞬发直出零打扰；耗时 > 400ms 屏幕顶端浮现轻量磨砂 HUD“正在识别文字…”，> 2s 提示“正在优化本地模型…”；采用 `.nonactivatingPanel` 零焦点抢占，并在结果弹出时精准淡出。

### 6.2 多维触发与原地平滑复用
- **触发途径**：
  - **全局快捷键直达**：按下 **`⌥ + 4`**（`Option + 4`，支持自定义），框选任意屏幕文字区域，松手瞬间即刻弹出半透明毛玻璃识别结果面板。
  - **菜单栏一键直达**：在菜单栏点击「文字识别 (OCR)」，框选松手直接出识别结果。
  - **截图标注 / 贴图提取**：点击工具栏 🔍 图标或按下 `⌥ + O`（`Option + O`）提取。
  - **全域拖拽本地图片识别 (v2.7.9)**：将任意格式图片文件（`.png`, `.jpg`, `.jpeg`, `.webp`, `.heic`, `.tif`, `.bmp`, `.gif` 等）直接拖入 OCR 结果窗口的任意区域（标题栏、编辑区、底部操作栏、边框），窗口浮现全屏毛玻璃遮罩，松开鼠标自动提取文字并刷新；拖入纯文本时平滑追加至当前编辑区末尾。
- **原地平滑复用状态机 (In-Place Window Reuse, v2.7.9)**：
  - 连续截屏或识别时彻底废除窗口销毁重建机制，采用原地替换状态机，保持窗口物理位置与尺寸绝对稳定，原子更新文本、原图与条码，实现真正的 0 闪烁无缝更新。
- **多屏幕物理感知安全居中与 Clamping (v2.7.9)**：
  - 自动探测包含鼠标光标的物理 `NSScreen`，基于 `visibleFrame` 计算中心坐标并执行 16pt 安全边界裁切，窗口永远稳固出现在当前屏幕中央。

### 6.3 空间几何排版重构引擎 (Spatial Layout Preservation, v2.7.9)
- **阅读流空间几何重构**：翻转 Vision 坐标系为物理阅读流（`topY = 1.0 - box.maxY`），引入行高自适应垂直聚类算法（`tolerance = max(avgHeight, fragment.height) * 0.55`）。
- **表格与多栏排版还原**：结合图像物理宽高比智能还原文字块间的水平相对距离，自动插入制表符 `\t` 或空格，完美还原表格行列对齐与左右分栏图文排版，彻底解决文字碎裂成单列下落的排版痛点。

### 6.4 全能文本清洗与格式化套件 (Text Transform Suite, v2.7.7 ~ v2.7.9)
顶部快捷栏采用 4 个纯净极简的下拉分类菜单（「清洗」、「排版」、「大小写」、「标点」），在 420pt 最小窗口下零截断、零滚动：

```
┌────────────────────────────────────────────────────────┐
│ [清洗 ▾]   [排版 ▾]   [大小写 ▾]   [标点 ▾]      [✨ AI 解释] │
└────────────────────────────────────────────────────────┘
```

1. **「清洗」分类菜单**：
   - **去空格 (CharCategory 状态机)**：全面覆盖 CJK 汉字全扩展区、全半角标点、开闭括号与数字。确立清晰白名单机制：**仅在纯英文字母之间或英文字母与数字之间保留单个词距空格（如 `Hello World`、`iOS 18`）**，其余所有汉字、标点、括号内外、连续数字与符号间的空格 100% 彻底清除。
   - **去换行 (Merge Lines)**：智能合并段内断行，自动修复行尾被断开的英文连字符（Hyphen-breaking，如 `connec-` + `tion` $\to$ `connection`），智能保留自然段落与空行。
   - **清理全部空行 (Trim Empty Lines)**：剔除文本中所有连续冗余的空白行。
2. **「排版」分类菜单**：
   - **智能间距 (Pangu Spacing)**：自动在中文字符与英文单词、数字之间插入规范的空格分隔符，规范技术文档排版。
   - **转 Markdown 表格 (Idempotent GFM Table Engine)**：解析前自动过滤已有表格分隔行，支持按 `\t`、`|` 或连续多空格智能分列，自动补齐缺损单元格并转义内容中的 `|` 字符。具备**幂等性**保护，无论重复点击多少次均输出标准单层 GitHub Flavored Markdown 表格，可直接粘贴进 Obsidian、Notion、Typora 等编辑器。
3. **「大小写」分类菜单**：
   - **全部大写 (UPPERCASE)**
   - **全部小写 (lowercase)**
   - **首字母大写 (Title Case)**
4. **「标点」分类菜单**：
   - **标点转半角**：将全角标点符号（`，`、`。`、`！`、`？`、`：`、`；`、`（`、`）` 等）1:1 转换为标准 ASCII 半角标点。
5. **选区局部清洗感知**：
   - 鼠标高亮选中编辑器中的局部文字时，顶部自动浮现 `选区 (N)` 徽标，此时点击任意清洗或排版功能，仅对选区内文字执行局部格式化，未选中文本不受影响。

### 6.5 双模态智能撤销/重做快照引擎 (Dual-Mode Undo/Redo Engine, v2.7.7)
- **大颗粒突变原子快照**：执行一键文本清洗或从外部粘贴大段文本时，立即触发原子离散快照，按一次 `⌘ + Z` 或点击顶栏撤销即可瞬间完整回退整段粘贴或清洗。
- **连续微输入 500ms 句级聚合**：连续打字时启用 500ms 句级聚合防抖，避免撤销栈碎片化。
- **30 步 FIFO 快照栈**：基于 Swift Copy-on-Write 内存上限约束与 `@Published` 响应式流转，实现 O(1) 微秒级撤销/重做响应与按钮高亮联动。

### 6.6 二维码/条形码自动检测与智能横幅 (Barcode/QR Engine, v2.7.9)
- **并发条码检测**：后台并发执行 `VNDetectBarcodesRequest`。
- **智能胶囊横幅**：检测到二维码/条码时，结果窗口顶部平滑滑出操作横幅，显示码制与内容：
  - 若 Payload 为 URL 链接，提供一键 **【打开网址】**（Safari 直达）；
  - 提供一键 **【复制内容】** 按钮。
- **纯条码 Fallback 兜底**：截取纯二维码/条形码无普通文字时，自动回退将 Payload 作为主文本填充，杜绝“未检测到文字”误报。

### 6.7 连续追加截图模式与多图图库 (Continuous Append Mode, v2.7.9)
- **保序 FIFO 追加队列**：点击 Header 栏「追加模式」按钮开启。开启后连续截屏，识别结果将自动换行拼接在本文末尾，适合长篇文献、多页 PPT 或分段代码集中归纳。
- **多图图库胶囊**：开启原图对照时升级为多图图库胶囊（`[1] [2] [3*]`），支持自由点击切换回溯任意历史截图。
- **单步原子撤销**：每次追加操作均为独立原子快照，按下 `⌘ + Z` 可精准单步撤销最后一次追加的内容与图片。

### 6.8 AI 深度流式解析与打字机效果 (Streaming SSE Typewriter, v2.7.7)
- **40ms 动态节流流式打字机**：点击「✨ AI 解释」，通过 Server-Sent Events 流式接收 Chunk，配合 40ms（25 FPS）动态节流阀驱动 `@Published`，呈现逐字丝滑打字机动画，主线程零掉帧。
- **🧠 思考过程分流折叠**：自动提取推理大模型（如 DeepSeek-R1）的 `<think>...</think>` 标签，收纳至可自由展开/折叠的「🧠 思考过程」灰度卡片；点击「复制解析」自动剔除思考标签，只导出纯净干货。
- **原生 Markdown 深度排版**：正文接入 `AttributedString(markdown:)` 渲染加粗、段落、列表、代码块与表格，内置未闭合代码块自动修补容错。
- **多轮连续上下文追问 (Multi-Turn Chat)**：
  - 面板底部集成独立追问输入条，输入指令后按回车发起多轮连续追问；
  - System Prompt 强制锚定注入当前 OCR 原始内容，结合 6 轮滑动窗口与思考过程剥离，既保持原文字准确记忆，又实现极低 Token 消耗；
  - 追问输入框具备严格焦点隔离，打字撤销绝不干扰上方 OCR 编辑器。
- **单向布局边界锁定**：AI 面板展开时编辑区在窗口内部弹性让出空间，窗口外部尺寸绝对恒定；消息列表设定 140pt 物理视口高度，鼠标滚轮顺畅滚动并支持流式自动滚底。

### 6.9 高效快捷操作与导出
- **快捷键 `⌘ + ↩` (Command + Return)**：一键复制全文并关闭 OCR 窗口。
- **快捷键 `Esc`**：快速关闭 OCR 窗口。
- **等宽字体切换**：Header 支持一键切换常规字体与代码等宽字体（Monospaced）。
- **原图对照分屏**：Header 支持一键展开/折叠原图对照分屏。
- **导出文件**：Header 支持一键导出为纯文本文件。

---

## 7. 矢量标注与智能图形体系

### 7.1 丰富标注工具箱与高级马赛克渲染管线 (v2.8.2 核心重构)
- **矩形 / 椭圆 / 箭头 / 直线 / 折线**：高保真抗锯齿与精准几何拾取。
- **自由画笔 (Pen) & 记号笔 (Highlighter)**：高保真贝塞尔曲线与笔刷透明度叠层。
- **区域均值马赛克渲染管线 (v2.8.2)**：
  - **区域均值下采样与最近邻点阵上采样 (Area-Averaged Downsample & Nearest-Neighbor Upsample)**：废除 Core Image `CIPixellate` 单点采样导致的“白底黑字中心采空白背景致使整行文字被 100% 抹白擦除”痛点；构建 `generatePixelatedImage` 纯函数，先以面积积分均值插值将细小文字笔画与背景平滑混合为灰度均值，再以无插值点阵放大，生成边缘分明、颗粒清晰的经典方块马赛克网格（**耗时 < 2ms**），完美保留文字排版密度与灰度质感。
  - **时序游程分块与连续涂抹防崩 (Chronological Chunking & Mode Guard)**：引入 `MosaicChunk` 游程分块渲染管线，隔离单次笔刷；支持单点点击打码与连续多次独立涂抹，绘制模式下流式笔刷跳过手柄检测，互不干扰。
  - **特征能力解耦与二级工具栏纯净化 (Trait-Based Toolbar Decoupling)**：模型层抽象 `AnnotationTool.supportsColor` 矩阵。马赛克模式下自动隐藏无意义的 8 色调色板，仅展示【马赛克样式切换 + 一键智能脱敏 + 笔刷粗细调节】，右侧自适应呈现中性半透明圆环粗细预览，彻底杜绝误导。
- **三档打码样式**：支持 **像素化方格 (Pixelate)**、**平滑高斯模糊 (Blur)** 与 **纯黑遮挡 (Blackout)**。

### 7.2 智能引线批注工具 (Remark Leader Notes)
- **双点锁定与毛玻璃说明卡片**：第一点为目标锁定锚点（带白边精致圆点），第二点为可随意拖拽的说明卡片。
- **Ray-AABB 射线自适应边缘停靠**：引线与卡片四边无论如何拖动，均根据几何射线相交算法实现 100% 顺滑停靠，支持直线与 90° 直角折线两种视觉风格。
- **120fps 实时动态包裹与微型初始光标胶囊**：空文本初始锁定为 $28 \times 24\text{pt}$ 微型光标胶囊，击键输入时卡片背景与引线交点以 120fps 实时向外平滑呼吸扩张。

### 7.3 黄金比例自适应排版与四向边界防溢出
- **三阶黄金比例排版**：基于字符面积与黄金比例（短文本 2.6:1 药丸状、中长文本 1.618:1 黄金分割、长文本 1.45:1 紧凑矩形）推导最优宽度，彻底告别细长面条文本。
- **四向画幅边界平滑防溢出吸附**：在截图框边缘输入多行文字时，自动平滑向上/向内反推收纳，绝不刺穿画幅或遮挡工具栏。
- **交互优先级防线**：单次点击空白处优先提交编辑并阻断误创建，空文本自动回收。

### 7.4 精准指向型水滴序号图钉与原子连续重排
- **尖端物理锚点水滴图钉 (Pointer Step Pin)**：拥有锐利尖端锚点，支持绕尖端 $360^\circ$ 任意旋转（旋转零位置漂移），完美解决密集 UI 元素中纯圆图钉指向模糊的问题。
- **双样式自由切换**：工具栏支持随时切换【经典圆形】与【水滴指向】双样式。
- **原子序号连续重排引擎 (Atomic Step Reordering)**：删除或撤销某个特定序号图钉时，后续编号自动递补重排（如删除 2，原 3, 4 自动变为 2, 3），流程说明序号永远连续。
- **多态几何碰撞矩阵**：水滴动态多边形 `path.contains` + 圆形 `hypot` 检测，100% 精准拾取。

### 7.5 智能手绘图形自矫正 (Shape Recognizer)
- 基于 Ramer-Douglas-Peucker (RDP) 算法与几何偏差计算：
  - 随手画圈 $\to$ 自动矫正为完美正圆/椭圆；
  - 随手画框/三角 $\to$ 自动矫正为几何多边形；
  - 随手画折线 $\to$ 自动平滑为直线。

### 7.6 一键隐私脱敏 (Auto-Redact Engine)
- 结合 Apple Vision 人脸识别与自然语言数据探测器（`NSDataDetector` / Regex）：
- 一键自动识别并对**人脸、手机号、身份证号、银行卡号、邮箱地址**打上安全马赛克。

---

## 8. 全局平铺防泄密水印滤镜 (Global Watermark) `[v2.5.8 ~ v2.6.9 增强]`

### 8.1 全画幅滤镜级平铺覆盖与防丢闭环 (v2.6.9 重构)
- **零误触底层架构**：水印作为底层全局滤镜铺满整个截图区域。用户在画满水印的截图上继续添加箭头、框选或文字标注时，绝不会误触或拖乱水印。
- **合成链路全局挂载**：保存图片、复制到剪贴板、固定为贴图（Pin Window）及长截图导出全链路挂载水印合成，贴图窗口克隆母体水印配置，彻底根除水印丢失问题。

### 8.2 专属二级交互控制面板与防截断算法
- **一键实时开关与反选 (v2.6.9)**：工具栏点击【水印】图标开启全局水印，再次点击该图标直接联动关闭水印；鼠标指针重设为常规 `.arrow` 消除绘图误导。
- **多向倾斜角度预设**：支持 **0° (水平)、45° (向右下倾斜)、-45° (向右上倾斜)** 三种预设角度一键切换。
- **网格化疏密平滑调节**：通过连续滑块实时改变水印平铺的密集程度，所见即所得。
- **基于对角线包围盒的防截断弹性画布 (v2.6.9)**：引入 `sqrt(w^2 + h^2) + padding` 动态几何重算，长文本与高密度平铺下文字绝不被裁切。

### 8.3 GPU 硬件原生渲染
- 底层采用 SwiftUI `ImagePaint` 瓦片技术，单张基础图元由 GPU 硬件原生平铺缓存，无论是多复杂的屏幕或 120fps 缩放拖拽，**CPU 额外占用为 0**。

---

## 9. 一键智能抠图与净底工作台 (Matting Studio)

### 9.1 三路智能分流路由矩阵 (Triple-Engine Routing Matrix)
AuraSnap 构建了三路兼顾极致锐利与高保真连续 Alpha 蒙版的智能分流体系：

1. **Pass 1: 工业级拉普拉斯高频能量净底体系 (UI 截图)**
   - **适用场景**：纯色背景、代码截图、图标、带阴影的 App 卡片与文字 Logo（色彩独特性比例 $\le 5\%$）。
   - **核心原理**：基于二阶拉普拉斯高频能量场与感知加权色差，从物理数学上分离高频文字笔画与低频平滑阴影。
   - **效果**：**100% 保留文字笔画锐度与边缘反锯齿**，彻底杜绝深度学习模型抠文字时的发虚、毛刺与模糊。
2. **Pass 2: 高精云端商业级语义去背 (兼容 Bria API)**
   - **适用场景**：人像、复杂宠物毛发、半透明玻璃与复杂自然实物。
   - **核心原理**：支持用户接入合规的 Bria AI 云端 API 接口，输出连续的高精度 Alpha 透明蒙版。
   - **体验**：高精度连续 Alpha 蒙版自然过渡复杂边缘与半透明区域，告别白边锯齿。在“偏好设置 $\to$ AI 助手”中用户可自由配置自有 Bria API Key 与 Base URL，支持一键“测试连接”。
3. **Pass 3: Apple Vision 原生语义分割 (离线终极兜底)**
   - **适用场景**：无网络环境、未配置云端 Key 或 API 异常时。
   - **核心原理**：调用本地 `VNGenerateForegroundInstanceMaskRequest` 毫秒级提取主体。
   - **效果**：零延迟、零网络依赖、零额外包体积，确保一键抠图功能 **100% 高可用**。

### 9.2 交互特技与双模式
- **GPU 硬件加速透明棋盘格 (Transparency Checkerboard)**：8×8pt 微型交替网格，暗色主体与黑色文字对比强烈、清晰可见。贴图抠图后同步渲染物理棋盘格并拦截事件穿透 (v2.6.7)，保持自由点按与拖拽手感。
- **原位去背 (In-Place Cutout) 与 0 延迟一键复原**：选区或贴图点击【抠图】原位去背，已有标注零位移；再次点击立即 0 延迟复原原图。
- **贴图仅复制主体**：贴图右键菜单支持【仅复制主体】，直接将透明底 PNG 复制到剪贴板。

---

## 10. AuraVision AI 视觉大模型与输入清洗

### 10.1 多模型自由接入
- 兼容任何 OpenAI 协议端点：
  - **SiliconFlow (SiliconCloud)**
  - **OpenAI (GPT-4o, GPT-4.5)**
  - **Claude 3.5 Sonnet / Opus**
  - **DeepSeek-V3 / DeepSeek-R1**
  - **Google Gemini 1.5 Pro / Flash**
  - **本地 Ollama 离线多模态大模型**
- 支持自定义 `Base URL`、`API Key` 与 `Model Name`。

### 10.2 不可见字符与零宽格式自动清洗引擎 (v2.6.0)
- **彻底告别幽灵 403/401 报错**：从网页、PDF 或富文本编辑器复制 API Key / Base URL 时，剪贴板常夹带零宽空格 (`\u200B`)、双向标记 (`\u200E`)、非断行空格 (`\u00A0`) 等隐式字符，导致 Swift URL 编码畸变。
- **底层双向清洗**：内置 `String.cleanAIInput` 拦截器，在粘贴瞬间自动过滤所有乱码与隐式控制字符，将纯净文本持久化写入 Keychain 与 UserDefaults。

### 10.3 预设动作与“标注感知 (Annotation-Aware)”
- **代码解释 / 逻辑剖析 (Explain)**：精准分析框选代码与界面；若图片中绘制了**红圈、箭头或框线**，系统指令会自动引导 AI **聚焦于被标注的核心区域**！
- **纯粹翻译 (Translate)**：结合本地 Apple 神经引擎高精度 OCR 与大模型，直接输出纯净翻译文本，无废话语气词。
- **排版级 OCR (OCR to Markdown/Code)**：严格保持缩进与 Markdown 表格格式输出。
- **自由连续追问**：支持任意指令多轮追问对话，上下文仅保留纯文本历史（**0 显存与图片堆积**）。

### 10.4 四向几何智能避障 (4-Way Avoidance)
- 严格遵循 **下方外部 $\to$ 上方外部 $\to$ 右侧外部 $\to$ 左侧外部** 几何推演，30pt 防颠簸死区，100% 紧贴选区外沿，永不遮挡截图画面。

---

## 11. 分模块独立主题配置与高对比 HUD 渲染 `[v2.6.2 ~ v2.7.8 架构升级]`

### 11.1 分模块独立主题解耦 (Modular Independent Themes)
将外观渲染管线彻底解耦为「主工具栏」与「OCR 结果窗口」两个完全独立的配置维度，分别通过 `@AppStorage` 独立持久化存储：
- **主工具栏主题 (Toolbar Theme)**：默认推荐 **通透玻璃 (Liquid Glass)**，兼顾轻透水滴质感与标注沉浸感。
- **OCR 结果窗口主题 (OCR Window Theme)**：默认推荐 **高对比度 (HUD)**，深色厚磨砂抗底图反光，保障大段文字阅读的极致对比度。

### 11.2 三档标准纯净风格
1. **通透玻璃 (Liquid Glass)**：
   - **macOS 26+ 原生双引擎**：直接接入系统级 `.glassEffect(Glass.clear.tint(...))`，搭配 `12% 微暗调吸光滤镜` 与纯白文字微米级动态投影（Drop Shadow），在纯白网页与深色背景下均呈现高级水晶折射与锐利字符。
   - **macOS < 26 拟态兼容层**：基于 Popover 磨砂与线性渐变外描边自适应渲染。
   - **系统级颜色穿透**：底层注入 `.preferredColorScheme(.dark)` 穿透系统级 `NSMenu` 弹出列表，消灭白底白字。
2. **高对比度 (HUD, Native .hudWindow Frosted)**：
   - 接入 AppKit 原生 `NSVisualEffectView(material: .hudWindow, blendingMode: .behindWindow)`。
   - 利用 HUD 材质的大模糊半径与深层光线吸收特性，彻底过滤浅色高光、复杂桌面或代码界面的底层高频噪点与杂乱反光，大幅提升文字边缘对比度与可读性。
3. **经典纯色 (Classic)**：
   - 极简扁平风格，跟随系统浅色/深色主题标准纯色底板。

### 11.3 系统级原生下拉菜单设置
- 偏好设置「常规」面板采用 Apple HIG 规范的原生 Menu 下拉菜单（`.pickerStyle(.menu)`），与系统控件保持一致的右对齐工整布局，即选即生效，无需重启应用。

---

## 12. 工具栏与菜单栏深度定制 `[v2.7.5 升级]`

### 12.1 菜单栏自定义配置与排序 (Menu Bar Customization)
在偏好设置【工具栏】$\to$【菜单栏】管理面板中，支持对所有菜单项进行可视化管理：
- **功能项一键显隐**：支持独立勾选/取消勾选任意菜单项（截取屏幕、滚动截长图、录制动图、文字识别 OCR、截图历史记录、恢复最近贴图、剪贴板转贴图、偏好设置、检查更新、退出 AuraSnap）。
- **拖拽自由排序**：按住右侧拖拽手柄即可上下调整菜单项在系统菜单栏下拉列表中的排列顺序，配置实时同步。
- **全关安全保底机制**：内置分类分割线与安全防线，防止误关所有菜单项。

### 12.2 智能直达模式 (Smart Direct Capture Pipeline)
打通菜单栏项与全局快捷键的极速直达分发管线，省去二次点击步骤：
- **文字识别直达**：在菜单栏点击「文字识别 (OCR)」或按下 **`⌥ + 4`**，框选目标区域松手直接弹出 OCR 结果窗口。
- **录制动图直达**：在菜单栏点击「录制动图 (GIF)」，框选松手直接锁定选区并唤起录屏控制栏。
- **滚动截长图直达**：在菜单栏点击「滚动截长图」，框选松手直接锁定选区并唤起长截图控制条。

### 12.3 截图与贴图双工具栏独立定制
- 在偏好设置【工具栏】中提供 **【首次截图工具栏】** 与 **【二次编辑/贴图工具栏】** 两个独立面板。
- **工具显隐自由开关**：矩形、圆形、箭头、直线、引线批注、画笔、荧光笔、文本、水印、抠图、马赛克、水滴序号、长截图、录屏、AI、贴图、OCR、保存、复制等工具均配有独立 Toggle 开关。
- **鼠标拖拽自由排序**：按住三横线手柄上下拖动即可自定义工具栏按钮顺序。
- **跨屏幕拖拽与独立层级 (v2.6.8)**：工具栏升级为独立浮动级别（`.floating` + `.order(.above)`），可自由拖至多块物理显示器不隐身，原生位移追踪（`NSWindow.didMoveNotification`）记忆用户手动拖拽位置。

---

## 13. 全维度快捷键体系与单键直达系统 (Shortcut Architecture & Registry) `[v2.8.1 重构]`

### 13.1 单键标注工具秒级直达 (Single-Key Tool Direct Access, v2.8.1)
- **10 种标注工具全量单键切换**：
  - **`R`**：矩形选框 (Rectangle)
  - **`A`**：精准箭头 (Arrow)
  - **`M`**：马赛克 / 高斯模糊 / 纯黑遮挡 (Mosaic)
  - **`P`**：自由画笔 (Pen)
  - **`L`**：直线 / 折线 (Line)
  - **`T`**：文本标注 (Text)
  - **`N`**：精准水滴序号图钉 (Number Pin)
  - **`B`**：智能双点引线批注 (Remark Note)
  - **`W`**：全局防泄密平铺水印 (Watermark)
  - **`V`**：选择与图元变换手柄 (Select)
- **贴图浏览态无缝激活**：在置顶贴图（Pin Window）浏览状态下，直接按下任意单键（如 `R` / `A` / `T`），系统**自动唤醒编辑模式并选中该工具**，无缝衔接二次标注。

### 13.2 文本打字焦点绝对安全放行第一防线 (Context-Aware Typing Barrier, v2.8.1)
- 底层构建 `ShortcutRouter.isTextInputFocused` 核心防线。
- 当光标聚焦于文本输入框（`NSTextView` / `NSTextField`，如文字标注打字、批注说明卡片编辑、水印文字修改时），**所有单键与热键 100% 绝对放行给系统原生输入**，彻底杜绝打字误触工具切换。

### 13.3 自研 60ms 瞬发全主题自适应 Tooltip 悬浮气泡 (ThemeTooltipBubble, v2.8.1)
- **彻底根除层级遮挡**：废除 macOS 原生 `.help(...)` 迟滞（1.5s）及被全屏窗口遮挡痛点，在工具栏同层渲染纯净毛玻璃悬浮气泡。
- **60ms 瞬发微防抖**：鼠标悬停 60ms 立即弹出，移开瞬间平滑淡出。
- **全主题色彩自适应**：自动跟随工具栏三大主题（通透液态玻璃、高对比度 HUD 磨砂白字高光、经典纯色）高保真自适应渲染。
- **KeycapBadge 实体按键微标**：内嵌精致实体按键微标，并实时联动 `ShortcutRegistry` 改键同步。

### 13.4 全局系统快捷键 (Global Hotkeys, v2.8.1 扩展)
底层采用 Carbon C-API 事件拦截，响应速度达到硬件级微秒级，均可在设置中自定义录制：
- **屏幕截图**：默认 `⌥ + 1`（`Option + 1`）
- **恢复贴图栈**：默认 `⌥ + 2`（`Option + 2`）
- **历史图库画廊**：默认 `⌥ + 3`（`Option + 3`）
- **文字识别 (OCR) 直达**：默认 `⌥ + 4`（`Option + 4`）
- **滚动截长图直达**：默认 **`⌥ + 5`**（`Option + 5`）`[v2.8.1 新增]`
- **录制动图 (GIF) 直达**：默认 **`⌥ + 6`**（`Option + 6`）`[v2.8.1 新增]`
- **剪贴板转贴图 (图文)**：默认 `⌘ + ⌥ + T`（`Cmd + Option + T`）
- **贴图穿透模式切换**：默认 `⌥ + G`（`Option + G`）

### 13.5 会话与局部动作快捷键 (Local Hotkeys)
- **单键标注切换**：`R` / `A` / `M` / `P` / `L` / `T` / `N` / `B` / `W` / `V`
- **一键提取主体 / 抠图**：默认 **`E`** (Extract) `[v2.8.1 新增]`
- **选区转长截图**：默认 **`S`** (Scroll) `[v2.8.1 新增]`
- **选区转录屏**：默认 **`G`** (GIF) `[v2.8.1 新增]`
- **复制截图 / 复制贴图 / 复制动图**：默认 `⌘ + C`
- **保存截图 / 保存贴图 / 保存动图**：默认 `⌘ + S`
- **固定为贴图 (Pin)**：默认 `⌘ + P`
- **文字识别 (选区/贴图提取)**：默认 `⌥ + O`（`Option + O`）
- **OCR 复制全文并关闭**：`⌘ + ↩`（`Cmd + Return`）
- **复制当前取色色值**：默认 `C`
- **撤销批注 / 撤销文本编辑 / 撤销追加**：`⌘ + Z`
- **重做文本清洗**：`⇧ + ⌘ + Z`
- **取消/退出截图 / 关闭浮窗**：`Esc` 或 鼠标右键

### 13.6 数据驱动配置中心与智能录制器 (ShortcutRegistry, v2.8.1)
- 统一管理所有元数据、用户自定义覆盖与 `UserDefaults` 响应式持久化。
- 偏好设置按「全局系统快捷键」、「标注绘图工具」、「会话操作」、「其他内置」四个分类卡片化清晰呈现，支持单键录制与一键「恢复默认快捷键」。

---

## 14. 历史图库画廊与离线 OCR 语义检索

- **网格画廊**：沉浸式浏览历史截图，支持自定义保留周期（1天/3天/7天/永久），过期自动清理。
- **离线 OCR 全文检索**：所有历史截图在本地通过 Apple 神经引擎完成离线文本提取，在搜索框输入文字即可毫秒级搜出包含该文字的历史截图。
- **按需懒加载**：历史记录采用磁盘缓存与按需加载机制，保存成千上万张截图依然保持超低内存占用。

---

## 17. 快捷键与高频交互手势全览表 (v2.9.0 最新版)

| 场景 / 模块 | 操作动作 | 功能说明 | 版本 |
| :--- | :--- | :--- | :--- |
| **全局** | `⌥ + 1`（可自定义） | 瞬间唤起屏幕截图与窗口微观吸附 | 核心 |
| **全局** | `⌥ + 2`（可自定义） | 恢复最近关闭的贴图（后进先出栈） | 核心 |
| **全局** | `⌥ + 3`（可自定义） | 打开历史截图画廊 | 核心 |
| **全局** | `⌥ + 4`（可自定义） | **极速直达文字识别 (OCR)：框选松手直接弹出识别结果** | `v2.7.5` |
| **全局** | **`⌥ + 5`**（可自定义） | **极速直达滚动截长图：框选松手直接唤起长截图控制条** | `v2.8.1` |
| **全局** | **`⌥ + 6`**（可自定义） | **极速直达录制动图 (GIF)：框选松手直接锁定选区并唤起录屏栏** | `v2.8.1` |
| **全局** | **`⌥ + 7`**（可自定义） | **极速直达背景美化：框选松手直接呼出独立背景美化窗口** | `v2.9.0` |
| **全局** | `⌘ + ⌥ + T`（可自定义） | **剪贴板转贴图 (图文双模态)：纯文本或复制图片一键转浮空贴图** | `v2.7.5` |
| **全局** | `⌥ + G`（可自定义） | 切换当前鼠标下贴图的穿透模式 (Click-Through) | 核心 |
| **截图前** | `Space` | 切换“宏观窗口吸附”与“微观 UI 元素吸附” | 核心 |
| **截图前** | 鼠标滚轮 | 在 UI 控件树中向上/向下切换探测层级 | 核心 |
| **截图前** | `C`（可自定义） | 复制当前光标处颜色的 HEX 与 RGB | 核心 |
| **截图前** | `↑ ↓ ← →` | 以 1px 步进微调十字准星位置 | 核心 |
| **选区/标注** | **`R` / `A` / `M` / `P` / `L`** | **单键直达：矩形 / 箭头 / 马赛克 / 画笔 / 直线** | `v2.8.1` |
| **选区/标注** | **`T` / `N` / `B` / `W` / `V`** | **单键直达：文本 / 序号图钉 / 引线批注 / 水印 / 选择** | `v2.8.1` |
| **选区/标注** | **`D`**（可自定义） | **单键直达智能内容擦除 (Eraser / Inpainting)** | `v2.9.0` |
| **选区/标注** | **`K`**（可自定义） | **单键直达贴纸标注系统 (Stickers)** | `v2.9.0` |
| **选区/标注** | **`F`**（可自定义） | **一键唤起独立背景美化工作台 (Beautifier Window)** | `v2.9.0` |
| **选区/标注** | **`E`**（可自定义） | **单键一键提取主体 / 抠图 (Extract)** | `v2.8.1` |
| **选区/标注** | **`S`**（可自定义） | **当前选区一键转滚动截长图 (Scroll)** | `v2.8.1` |
| **选区/标注** | **`G`**（可自定义） | **当前选区一键转录制动图 (GIF)** | `v2.8.1` |
| **选区中** | 双击选区 | 快速复制选区图片并退出截图 | 核心 |
| **选区中** | `⌘ + C` / `⌘ + S` / `⌘ + P` | 快速复制 / 保存 / 置顶贴图 | 核心 |
| **选区中** | `⌥ + O`（可自定义） | **快速对当前选区执行 OCR 文字识别** | `v2.7.4` |
| **选区中** | `⌘ + Z` | 撤销上一步批注图形 / 撤销擦除消除 | 核心 |
| **选区中** | `Esc` 或 单击右键 | 取消并退出本次截图 | 核心 |
| **批注/贴纸** | 悬停贴纸 + 滚轮 / 触控板轻扫 | **贴纸本体与虚线框物理级等比缩放 (16...128)** | `v2.9.0` |
| **批注/擦除** | 框选/圈选/涂抹消除敏感文字 | **自适应双模 Inpainting 消除，纯净保留白底/渐变** | `v2.9.0` |
| **美化窗口** | 底部横向滑动切换预设 | **5K 系统壁纸 / 4K 渐变 / K-Means 主色萃取 / 莫兰迪** | `v2.9.0` |
| **美化窗口** | 点击「返回截图」 | **免重捕秒级复用内存帧返回，双向无损同步最新标注** | `v2.9.0` |
| **批注** | 涂抹马赛克 | **区域均值像素化采样，彻底消除文字死白抹除，保留颗粒质感** | `v2.8.2` |
| **批注** | 马赛克二级菜单 | **特征能力解耦，自动隐藏颜色选择器，纯净展示样式与脱敏** | `v2.8.2` |
| **批注** | 拖动水滴图钉把手 | **水滴序号图钉绕尖端 $360^\circ$ 精准旋转** | `v2.5.9` |
| **批注** | 删除任意序号图钉 | **现有序号自动原子递补连续重排 (1,2,3...)** | `v2.5.9` |
| **批注** | 双点引线批注 | **Ray-AABB 射线自适应边缘停靠 + 120fps 动态包裹** | `v2.5.4` |
| **批注** | 再次点击水印图标 | **一键反选联动关闭全局水印层** | `v2.6.9` |
| **贴图** | 按单键 `R/A/M/P...` | **自动激活贴图编辑模式并选中对应标注工具** | `v2.8.1` |
| **贴图** | **按住 `⌘` + 拖拽** | **直接将贴图拖入微信/钉钉/飞书/Slack 发送，或拖入文件夹秒存** | 核心 |
| **贴图** | 普通拖拽 | 移动贴图窗口在屏幕上的位置 | 核心 |
| **贴图** | 双击空白处 | 快速关闭并销毁贴图窗口 | 核心 |
| **贴图** | 鼠标滚轮 / 双指捏合 | **0.3x ~ 10x 无级等比缩放 (未截断浮点时间线，零漂移)** | `v2.7.3` |
| **贴图** | `⌥` + 鼠标滚轮 | 10% ~ 100% 调节贴图不透明度 | 核心 |
| **贴图** | 鼠标中键单击 | 瞬间重置贴图缩放与透明度至 100% | 核心 |
| **贴图** | 右键单击 | 呼出贴图功能菜单（复制/保存/分享/编辑/穿透/仅复制主体/OCR） | 核心 |
| **贴图** | `⌘ + C` / `⌘ + S` / `⌘ + P` | **贴图窗口直接响应键盘复制、保存与转贴图** | `v2.7.1` |
| **贴图** | `⌥ + O` | **对当前贴图快速执行 OCR 文字提取** | `v2.7.4` |
| **贴图 (穿透)** | 长按 `⌥` | 临时唤醒穿透的贴图以便移动或调整 | 核心 |
| **文本贴图** | 点击文字外空白区域 | **字形轮廓级事件穿透，可直接拖拽移动窗口** | `v2.7.3` |
| **OCR 窗口** | 识别多语言混合文本 | **Apple Neural Engine 硬件级加速，0.1~0.4s 秒级识别** | `v2.8.0` |
| **OCR 窗口** | 拖入本地图片文件 | **全屏毛玻璃提示遮罩，松手自动提取图片文字并刷新** | `v2.7.9` |
| **OCR 窗口** | 拖入纯文本 | **自动将文本平滑追加至当前编辑区末尾** | `v2.7.9` |
| **OCR 窗口** | 点击「追加模式」 | **连续截屏自动保序追加拼接，`[1] [2] [3*]` 胶囊切换原图** | `v2.7.9` |
| **OCR 窗口** | 高亮选中文字 + 清洗 | **自动触发选区局部清洗与排版，未选中文本不受影响** | `v2.7.9` |
| **OCR 窗口** | `⌘ + ↩` (Command + Return)| **一键复制识别/清洗后的全文并退出窗口** | `v2.7.4` |
| **OCR 窗口** | `⌘ + Z` / `⇧ + ⌘ + Z` | **双模态撤销与重做 (单步回退大段粘贴/清洗/追加)** | `v2.7.7` |
| **OCR 窗口** | 点击条码横幅「打开网址」 | **自动调用浏览器跳转二维码中的 URL 链接** | `v2.7.9` |
| **全局** | **`⌥ + 8`**（可自定义） | **极速直达原地截图翻译：框选松手直接把选区内容替换为目标语言** | `v3.0.5` |
| **选区中** | **`⌥ + T`**（可自定义） | **当前选区一键原地翻译（擦除原文字 + 译文原字号重绘）** | `v3.0.5` |
| **贴图** | **`⌘ + E`**（可自定义） | **切换贴图二次编辑模式（标注 / 翻译 / 消除）** | `v3.0.14` |
| **贴图** | **`Space`** | **切换贴图编辑模式（二次编辑开/关）** | `v3.0.16` |
| **贴图** | **`E`**（可自定义） | **移除背景 / 恢复原图（计入每日抠图配额）** | `v2.9.0` |
| **贴图** | **`⌥ + T` / 右键「翻译」** | **对贴图内容原地翻译（自动进入编辑态，多贴图互不连带）** | `v3.0.14` |
| **翻译面板** | **「重新翻译」** | **强制刷新：跳过译文缓存全量重跑，混排块遮罩首发治愈回显** | `v3.0.14` |

## 18. 原地截图翻译 (In-Place Screenshot Translation) `[v2.9.6 ~ v3.0.16 核心功能]`

选区内的外文截图，一键把文字替换为目标语言——擦除、排版、重绘全自动，译文以原字号原地呈现。

### 18.1 触发方式与全流程 (v3.0.14 入口完备)

| 入口 | 操作 | 说明 |
| :--- | :--- | :--- |
| **工具栏按钮** | 选区拉出后点击工具栏「翻译」 | 选区就绪后一键翻译 |
| **选区快捷键** | `⌥ + T`（可自定义） | 选区内直接翻译 |
| **全局直呼** | `⌥ + 8`（可自定义） | 框选松手直接原地翻译，免点按钮 |
| **贴图** | 右键菜单「翻译」/ 工具栏按钮 / `⌥ + T` | 对贴图内容原地翻译（自动进入编辑态） |
| **重新翻译** | 翻译二级面板「重新翻译」 | 强制刷新：跳过译文缓存全量重跑，混排块遮罩首发 |

全流程：框选 → 确定选区 → 自动完成「文字识别 → 擦除原文字 → 译文排版重绘」，数秒内选区内的外文被替换为母语译文。

### 18.2 双轨翻译架构与引擎模式

在「设置 → 翻译模式」中切换，两种模式各有 5 个翻译引擎可选：

- **快捷翻译模式**：提供开箱即用的快捷翻译通道，无需申请任何 API Key 即可快速完成「OCR + 翻译 + 像素级擦除重绘」一体输出，0 本地计算开销，极速呈现。
- **自定义 KEY 智能管线（BYO-Key）**：使用本地 Vision 框架高精度 OCR + 神经网络翻译 + CoreText 原生排版重绘，译文清晰锐利、排版精度更高、数据隐私自主可控（自备服务商 Key）。

### 18.3 排版与重绘核心特性

- **原字号优先排版**：译文继承原文的字号、字重与颜色，版面观感与原图一致（v3.0.9）；
- **空间几何排版引擎**：CoreText 原生测绘 + 射线借宽 + 平衡折行，中英混排不溢出、不重叠（v3.0.6）；
- **代码标识符大小写校正**：行内代码 chip 的驼峰标识符（如 `listMarkerRects`）被 OCR 误读大小写时，自动二次校正后再翻译，译文拼写忠实原代码（v3.0.11）；
- **遮罩首发重试**：「重新翻译」对混排块自动改写请求形态，治愈云端对同文本的确定性回显（v3.0.14）；
- **三态显示切换**：译文 / 原文 / 对照一键切换，随时回看原文。

### 18.4 译文钉定与多贴图隔离 (v3.0.12 ~ v3.0.14)

- **译文钉定**：翻译生效后调整截图范围，译文与擦除补丁钉在原屏幕像素上不随框漂移；
- **多贴图隔离**：多个贴图各自独立翻译，互不连带；
- **译文切换显隐**：再次点击翻译按钮可在译文/原图间切换。

### 18.5 翻译配额

免费用户每自然日 **3 次** 翻译额度（与抠图/智能消除/OCR 各自独立计数，次日自动重置）；Pro 会员无限次。详见第 19 节。

## 19. AuraSnap Pro 会员与免费配额 (Pro Membership & Free Quota) `[v2.9.2 商业化 ~ v3.0.16 治理]`

### 19.1 免费用户每日配额

免费用户每天享有以下功能的免费额度（按自然日自动重置，无需任何操作）：

| 功能 | 每日免费次数 |
| :--- | :--- |
| 智能主体抠图 | 3 次 |
| 智能内容消除 | 3 次 |
| OCR 文字识别 | 3 次 |
| 原地截图翻译 | 3 次 |

- 工具栏悬停提示实时显示**今日剩余次数**；剩余 ≤2 次时会主动气泡提醒；
- 额度耗尽后对应功能当天暂停，气泡会提示前往「设置 → 关于」开通 Pro；
- 其余基础功能（截图、标注、贴图、长截图录制、文本贴图等）免费不限量。

### 19.2 Pro 会员专属能力

升级 Pro 即解锁：全部每日配额功能**无限次**使用，外加 AI 视觉多模态工作台、背景美化与 3D 光影、60 FPS 高帧率录屏、长截图 PDF 导出、OCR 表格转 Markdown、多图追加工作台、敏感信息自动遮罩、手绘智能规整、自定义贴纸与 Pro 排版字体库等专属能力。

### 19.3 开通与激活

打开「设置 → 关于」，点击「开通 Pro 会员」前往购买页面（支持按年订阅与终生买断）；付款成功后激活码秒级发送至邮箱，在「关于 → 输入激活码」粘贴即可完成激活，亦支持直接粘贴 License Key 自动识别。

---

## 20. 径向分屏管理与智能邻接贴合 (Radial Window Manager) `[v3.0.24 新增]`

AuraSnap 引入了纯 Swift 原生构建的轻量级径向分屏管理引擎。通过极简的鼠标手势轮盘与纯键盘快捷键，让 macOS 窗口管理如丝般顺滑，彻底消灭第三方窗口管理软件的臃肿与延迟。

### 20.1 核心特性与 8 向灵动径向轮盘
- **160pt 毛玻璃灵动轮盘**：非激活式浮动面板（`NSPanel`），呼出瞬间不夺取当前前台 App 焦点，100% 零阴影零渲染伪影；
- **8 方向极速吸附**：以光标为中心向 8 个方向轻推鼠标，松手瞬间窗口即刻吸附：
  - **向左推 / 向右推**：左半屏 / 右半屏（50% 宽度）
  - **向上推 / 向下推**：上半屏 / 下半屏（50% 高度）
  - **左上 / 右上 / 左下 / 右下**：四角 1/4 屏幕贴靠
  - **中心稍推**：全屏最大化（铺满当前显示器工作区）
  - **中心微移**：居中窗口（保持原始尺寸或黄金比例居中）
- **半透明落点预览框 (Footprint Overlay)**：在滑动过程中实时高亮半透明落点示意框，所见即所得。

### 20.2 多样化触发手势与死区防误触
- **鼠标中键触发**：按住滚轮中键滑动即可唤起轮盘；
- **鼠标侧键触发**：内置智能侧键录制器，支持一键捕获并绑定鼠标按键 4 或按键 5；
- **键盘修饰键触发**：支持 `按住 Fn 滑动`、`按住 ⌥ 滑动` 或 `按住 ⌃⌥ 滑动`；
- **滑动触发死区调节 (10~35pt)**：有效过滤日常使用修饰键时的微小鼠标抖动，杜绝误触；
- **纯键盘模式**：可随时关闭手势轮盘，仅使用键盘快捷键极速分屏。

### 20.3 智能邻接贴合 (Smart Complementary Tiling)
- **消除小屏与非对称窗口重叠**：当某个分屏应用具有系统级最小尺寸限制（如微信、音乐等不可无限压缩宽度）时，邻接贴合引擎会自动感知相邻窗口，智能微调邻近窗口尺寸并实现无缝对齐铺满，彻底消除重叠遮挡；
- **台前调度 (Stage Manager) 自动避让**：在系统开启台前调度时，分屏自动在屏幕侧边预留缩略图安全区；
- **窗口贴边与间隙调节 (Window Gaps)**：支持 0~24pt 自由调整窗口之间与屏幕边缘的间距。

### 20.4 分屏默认快捷键速查

| 分屏操作 | 默认快捷键 | 手势方向 |
| :--- | :--- | :--- |
| **左半屏** | `⌃ + ⌥ + ←` | 向左推 |
| **右半屏** | `⌃ + ⌥ + →` | 向右推 |
| **上半屏** | `⌃ + ⌥ + ↑` | 向上推 |
| **下半屏** | `⌃ + ⌥ + ↓` | 向下推 |
| **全屏最大化** | `⌃ + ⌥ + ↩` | 中心稍推 |
| **居中窗口** | `⌃ + ⌥ + C` | 中心微移 |
| **左上 1/4 屏** | `⌃ + ⌥ + U` | 左上推 |
| **右上 1/4 屏** | `⌃ + ⌥ + I` | 右上推 |
| **左下 1/4 屏** | `⌃ + ⌥ + J` | 左下推 |
| **右下 1/4 屏** | `⌃ + ⌥ + K` | 右下推 |

---

## 21. 鼠标增强、平滑滚动与线性指针 (Mouse & Cursor Enhancement) `[v3.0.24 新增]`

为普通外接鼠标带来媲美 Apple Magic Trackpad 触控板的顺滑体验，一站式解决 macOS 外接鼠标滚轮生硬、方向绑定及加速度漂移的历史痛点。

### 21.1 模拟触控板平滑滚动 (Smooth Scroll)
- **触控板级惯性算法**：将传统机械滚轮阶梯式的生硬跳动转化为类似触控板的高帧率平滑过渡；
- **平滑度与滚动速度档位调节**：支持精细化级别调节，适配不同滚轮手感；
- **精准滚动模式 (Precision Scroll)**：在代码编辑器、设计画布与长文档中提供像素级平稳滚动。

### 21.2 独立反转滚轮方向 (Reverse Scrolling)
- **鼠标与触控板方向解耦**：单独反转外接鼠标的滚轮滚动方向，内置触控板双指滑动方向保持不变；
- **冲突检测与提示**：自动识别后台运行的 Mos、LinearMouse 等第三方滚轮工具，给出温馨提示防止反转冲突。

### 21.3 线性指针移动 (Linear Pointer / 消除加速度)
- **1:1 像素级精准跟手**：消除 macOS 默认的非线性鼠标加速度曲线，指针移动距离与物理鼠标位移绝对成正比；
- **专为设计与游戏优化**：在 Figma/Sketch 精准修图、CAD 绘图及 FPS 游戏中提供稳定的肌肉记忆手感；
- **指针速度多档可调**：支持线性模式下的指针灵敏度调节。

### 21.4 鼠标物理按键录制与功能映射 (Button Remapper)
- **全局硬件信号录制**：按下鼠标上的任意物理按键（中键、侧键 4/5 等），系统自动识别按键编号与修饰键；
- **灵活绑定核心操作**：可将鼠标侧键自由映射为屏幕截图、屏幕贴图、OCR 文字识别、背景美化、原地翻译、径向分屏等任意 AuraSnap 动作，单手掌控效率工作流。

---

